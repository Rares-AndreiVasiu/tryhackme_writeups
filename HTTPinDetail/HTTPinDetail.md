### What is HTTP(S) section

> the flag is shown after you click the lock for http, not secure website *THM{....}*, for invalid http certificate.

### Requests and responses section

What HTTP protocol is being used in the above example?

> *HTTP/1.1* as seen from the example request

What response header tells the browser how much data to expect?

> *Content-Length* response header tells the browser how many bytes of data to expect
